import { DisplayWindSpeedPipe } from './display-wind-speed.pipe';

describe('DisplayWindSpeedPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayWindSpeedPipe();
    expect(pipe).toBeTruthy();
  });
});
