import { DisplayTempPipe } from './display-temp.pipe';

describe('DisplayTempPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayTempPipe();
    expect(pipe).toBeTruthy();
  });
});
